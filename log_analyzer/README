Overview
This script for otus python course homework.
This script analyze nginx log file which writtine in following format:
'$remote_addr $remote_user $http_x_real_ip [$time_local] "$request" $status $body_bytes_sent "$http_referer" '
'"$http_user_agent" "$http_x_forwarded_for" "$http_X_REQUEST_ID" "$http_X_RB_USER" $request_time';


Usage:
1. Command line mode
python log_analyzer.py cmd

2. Config file mode
python log_analyzer.py config


Run Tests:
cd tests
python -m unittest